<h2>商品一覧</h2>

<% @items.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= image_tag attachment_url(item, :image, :fill, 120, 120, format: "jpg") %>
    <br>
    <%= item.name %>
    <br>
    ¥<%= item.price %>
    <br>
  <% end %>
<% end %>