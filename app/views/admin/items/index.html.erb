<h2>商品一覧</h2>

<% @items.each do |item| %>
  <%= item.id %>
  <%= link_to edit_admin_item_path(item.id) do %>
    <%= item.name %>
  <% end %>
  <%= item.price %>
  <%= item.genre.name %>
  <% if item.is_active == true %>
    販売中
  <% else %>
    販売停止中
  <% end %>
  <br>
<% end %>